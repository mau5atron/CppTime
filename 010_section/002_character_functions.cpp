#include <iostream>

int main(void)
{
  return 0;
}

/*
  Character Functions

  #include <cctype>

  function_name(char)

  - Functions for testing characters
  - Functions for converting character case

  - <cctype> inclues simple and useful functions that allow the testing of
    characters for various properties as well as the conversion of characters
    from upper to lower or lower to upper case in order to use these functions.
  - <cctype> functions expect a single character.

  Testing characters: 

  isalpha(c) - True if c is a letter
  isalnum(c) - True if c is a letter or a digit 
  isdigit(c) - True if c is a digit
  islower(c) - True if c is lowercase letter
  isprint(c) - True if c is a printable character 
  ispunct(c) - True if c is a punctuation character
  isupper(c) - True if c is an uppercase letter
  isspace(c) - True if c is whitespace 

  Converting characters: 

  tolower(c) - Returns lowercase of c
  toupper(c) - Returns uppercase of c
*/ 