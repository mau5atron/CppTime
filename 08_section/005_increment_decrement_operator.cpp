#include <iostream>

using namespace std;

int main(void)
{
  /*
    Increment++ || ++Increment
    Decrement-- || --Decrement

    Increments or decrements its operand by 1
    Can be used with integers, floating point types and pointers

    Prefix: ++num
    Postfix: num++

    Note: Don't overuse this operator (lol)
    ALert (lol) Never use it twice for the same variable in the same statement 
  */ 

  int counter {10};
  int result {0};
  return 0;
}