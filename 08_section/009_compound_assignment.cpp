#include <iostream>
using namespace std;

int main(void)
{
  /*
    Operator      Example       Meaning
      +=         lhs += rhs     lhs = lhs + (rhs);
      -=         lhs -= rhs     lhs = lhs - (rhs);
      *=         lhs *= rhs     lhs = lhs * (rhs);
      /=         lhs /= rhs     lhs = lhs / (rhs);
      %=         lhs %= rhs     lhs = lhs % (rhs);
      >>=        lhs >>= rhs    lhs = lhs >> (rhs);
      <<=        lhs <<= rhs    lhs = lhs << (rhs);
      &=         lhs &= rhs     lhs = lhs & (rhs);
  */ 
  /*
    for bitwise operator stuff 
    go to https://www.learncpp.com/cpp-tutorial/38-bitwise-operators/
  */ 
  
  return 0;
}