/*
  Section 19 Quiz

  1. The _______ class can be used to create files that can be written to, but
     not read from?
    
    answer: ofstream (output file stream)

  2. The ______ class can be used to create files that can be both written to
     and read from?

    answer: fstream

  3. The ____ method reads in an entire line from a text file.

    answer: getline()

  4. The ______ method writes a single character to a text file

    answer: put()

  5. You must #include _______ in order to perform file I/O in C++

    answer: <fstream>

  6. Which of the following stream manipulators is not persisten?

    answer: setw()

  7. Which class can be used to write formatted data to an in-memory string
     object?
  
    answer: stringstream

  8. By default, when we open a file for output, the contents of that file are
     ______.
    answer: truncated

  9. When are the steps, in order, that should be used when using a file in C++?

    - open the file, check if opened successfully, read/write data, close the
      file
    
  10. Closing an output file ______.
    - Should always be done since it writes out any unwritten buffers
*/